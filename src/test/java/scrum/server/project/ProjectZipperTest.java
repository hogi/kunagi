package scrum.server.project;

import org.testng.annotations.Test;

public class ProjectZipperTest {

	@Test
	public void zip() {
	// ProjectZipper zipper = new ProjectZipper(new File("runtimedata"), new
	// File("testng-output/projectzipper"),
	// project);
	// File file = zipper.createZip();
	// Assert.assertTrue(file.exists());
	// file.delete();
	}

}
